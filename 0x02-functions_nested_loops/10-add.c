#include "holberton.h"
/**
 * add - function that adds two integers and returns the result.
 * @m: variable type int
 * @p: variable type int
 * Return: returns the result.
 */
int add(int m, int p)
{
	int x = 0;

	x = m + p;
	return (x);
}
